function addTask(projectName, title, description, dueDate, priority) {
  //   let task;
  //   storage.saveTask(task, project);
}
function editTask(
  projectName,
  taskIndex,
  title,
  description,
  dueDate,
  priority,
  project
) {}
function removeTask(params) {}

function addProject() {
  //   let project;
  //   storage.saveProject(project);
}
function renameProject(projectName, newName) {
  //copy to new property then delete the old one
}
function getAllProjects() {
  //return
}
function getAllTasks(project) {
  //return Array of tasks
}
function removeProject() {}
export {
  addTask,
  editTask,
  removeTask,
  addProject,
  renameProject,
  removeProject,
  getAllProjects,
  getAllTasks,
};
